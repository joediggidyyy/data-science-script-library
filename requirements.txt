# This file intentionally defines the *core* dependency set.
#
# Install core:
#   pip install -r requirements.txt
#
# Install everything (including optional heavier deps like matplotlib/sklearn):
#   pip install -r requirements-full.txt

-r requirements-core.txt
