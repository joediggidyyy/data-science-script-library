# Benchmarks

Lightweight benchmarking utilities for learning and quick sanity checks.

## Included scripts

- `benchmark_simulator.py` — Loads a graph JSON and runs randomized access/traversal/path-finding benchmarks.

## Usage examples

Run the simulator against a graph JSON file:

```text
python scripts/benchmarks/benchmark_simulator.py --graph graph.json
```

