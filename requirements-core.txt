# Core dependencies (lightweight)
#
# This set is intended to support the core notebook + reporting workflows without
# pulling in heavier ML/plotting stacks.

nbformat>=5.9
nbconvert>=7.10
traitlets>=5.9
reportlab>=4.0
numpy>=1.23
